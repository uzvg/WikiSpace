created: 20240715193754172
modified: 20240715193923965
tags: Python
title: print函数
type: text/vnd.tiddlywiki

* print() 函数输出给定参数的值。
** 除了可以以单一的表达式作为参数（比如，前面的计算器的例子），它还能处理多个参数，包括浮点数与字符串。它输出的字符串不带引号，且各参数项之间会插入一个空格，这样可以实现更好的格式化操作：
* 关键字参数 end 可以取消输出后面的换行, 或用另一个字符串结尾：
```
a, b = 0, 1
while a < 1000:
    print(a, end=',')
    a, b = b, a+b
```
